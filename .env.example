# Copy to .env for local runs. In GitHub Actions, set these as repo secrets
# instead (Settings -> Secrets and variables -> Actions) — never commit real keys.

GOALDIR_API_KEY=
ODDS_API_KEY=
API_FOOTBALL_KEY=
SPORTAPI_KEY=
